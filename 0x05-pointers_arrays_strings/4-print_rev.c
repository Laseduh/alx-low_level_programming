#include "main.h"

/**
 * point_rev - print string in reverse followed by new line
 *
 * Return: Always 0.
 */

void print_rev(char *str);
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
